package enums;

public enum Decision {
    //Item passes policy checks and is recommended for approval
    ALLOW,
    
    //Item shows moderate concern and requires policy review
    CONDITIONAL,
    
    //
    DISALLOW
}